# ranchOS — 3 Strands Cattle Co. (v6.3)

**Mapbox Dark/Satellite basemaps** (raster tiles using your token), 4 camera feeds with live timestamp overlay, telemetry, herd movement with breadcrumbs, and an EID Lite workflow.

## Run
```bash
docker compose up --build -d
# open http://<your-host-ip>:8081  (reachable on all interfaces)
```
